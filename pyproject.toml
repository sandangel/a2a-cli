[project]
name = "a2a-cli"
version = "0.1.0"
description = "Developer tooling for a2a-cli"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "invoke>=3.0.3",
    "pre-commit>=4.5.1",
]
