# agc config — copy to ~/.config/agc/config.yaml
# Run `agc agent use <alias>` to switch the active agent.

current_agent: rover

agents:
  rover:
    url: https://dev.genai.stargate.toyota/a2a/rover-agent
    description: Rover Agent (dev)
    oauth:
      client_id: https://dev.genai.stargate.toyota/a2a/agc/.well-known/client-metadata.json
